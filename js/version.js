/**
 * Tek sürüm kaynağı — ?v= / SW / footer artırırken ÖNCE burayı güncelle.
 * Sonra: service_worker.js (SW_VERSION + urlsToCache), index.html, kasa.html ?v= değerleri.
 */
var SAHSI_ASSET_VERSION = '78.99';
var SAHSI_FOOTER_VERSION = '78.99';
